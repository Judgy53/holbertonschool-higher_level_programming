-- Script that lists all records of the table second_table of the current database, ordered by score

-- Select all score and name and order by score (bigger first)
SELECT score, name FROM second_table ORDER BY score DESC
